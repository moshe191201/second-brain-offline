# Domains

**The single copy of the domain list.** Produced by [`01-domains/A1`](01-domains/QUESTIONS.md);
read by every other step — classification, filtering, translation, and success criteria
all scope their answers per domain.

> **Do not restate this list inside a step file.** A second copy drifts the moment A7's
> completeness loop adds a domain, and then two steps are scoped to different maps. Link
> here instead.

Keep this file to the bare list. The rest of the domain model lives with the questions
that produce it: subdomains in `01-domains/A2`, out-of-scope domains and why in `A3`,
authorities in `A4`, contested splits and their rulings in `A5`, hard/soft dependencies
in `A6`.

| Domain | One-line description | Found via | In scope? |
|--------|---------------------|-----------|-----------|
| | | | |

---

**Last updated:**
**By:**
**A7 completeness iteration:**
