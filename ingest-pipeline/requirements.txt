# Ingest-pipeline runtime dependencies.
#
# The vault framework is pure stdlib on purpose and must stay that way; this file
# is the pipeline's dependency surface and does not belong to it. Nothing here is
# ever installed into a vault.
#
# Build the air-gap bundle from this file ON WINDOWS. `pip download` on macOS or
# Linux resolves the wrong wheels and you only find out on the far side of the
# gap. See HANDOFF.md §4.2.

# stage 3 — conversion
pypdfium2
markitdown
extract_msg
vsdx
python-docx
python-pptx
beautifulsoup4

# stage 4 — domain terms
wordfreq==3.1.1

# stages 3 and 5 — config + LLM transport
PyYAML
requests

# Not pip-installable, must come through the bundle separately:
#   pandoc >= 3.0 · docling-serve + models (~506 MB) + tessdata
#   .NET SDK 8.0.424+ and NuGet OfficeIMO.OneNote 3.2.2 (OneNote helper)
#   YAP + model data, located via YAP_DIR  <- stage 5 cannot run without it
#   a local OpenAI-compatible server (vLLM) serving minimax-m2.7 and kimi-k2.7
#   an embedding model for stage 6 retrieval
